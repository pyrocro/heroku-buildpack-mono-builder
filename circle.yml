machine:
  services:
    - docker
dependencies:
  cache_directories:
    - ~/git/mono/mono/.git
  override:
    - ./clone-if-not-exists
    - ./pull
test:
  override:
    - docker run -v ~/git/mono/mono:/tmp/mono heroku/cedar:14 ls /tmp/mono 