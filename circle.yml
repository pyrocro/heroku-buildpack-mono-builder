machine:
  node:
    version: 4.x
  services:
    - docker
dependencies:
  cache_directories:
    - ~/git/mono/mono/.git
  pre:
    - ./clone-if-not-exists
    - ./pull
test:
  override:
    - ./docker
deployment:
  github:
    branch: master
    commands:
      - ./deploy