dependencies:
  cache_directories:
    - mono-git
  override:
    - git clone https://github.com/mono/mono.git mono-git
    - pushd mono-git
    - git checkout -f 9e59d35315
    - popd
test:
  override:
    - echo "no build steps yet!"