machine:
  pre:
    - mkdir -p ~/git/mono/mono
dependencies:
  cache_directories:
    - ~/git/mono/mono
  override:
    - ./clone-if-not-exists
    - ./pull
test:
  override:
    - echo "no build steps yet!"
  post:
    - ./clean