machine:
  services:
    - docker
dependencies:
  cache_directories:
    - ~/git/mono/mono/.git
  override:
    - ./clone-if-not-exists
    - ./pull
test:
  override:
    - ./docker
#deployment:
#  github:
#    branch: master
#    commands:
#      - ./deploy