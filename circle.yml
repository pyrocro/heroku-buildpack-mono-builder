dependencies:
  cache_directories:
    - mono-git
  override:
    - ./clone-if-not-exists
    - pushd mono-git
    - git pull
    - git checkout -f 9e59d35315
    - popd
test:
  override:
    - echo "no build steps yet!"
  post:
    - pushd mono-git
    - git rm -rf .
    - git clean -fxd
    - popd