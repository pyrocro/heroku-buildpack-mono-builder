machine:
  services:
    - docker
dependencies:
  cache_directories:
    - mono/.git
  override:
    - ./get-mono
test:
  override:
    - ./build-mono
deployment:
  github:
    branch: master
    commands:
      - ./deploy-mono